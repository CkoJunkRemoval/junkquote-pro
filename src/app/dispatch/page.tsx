import AppLayout from"@/components/layout/AppLayout";import DispatchCenter from"@/features/dispatch/DispatchCenter";import{requireCompanyRole}from"@/lib/auth/tenant";import{getDispatchData}from"@/lib/dispatch/dispatch";
export default async function DispatchPage({searchParams}:{searchParams:Promise<{date?:string}>}){const context=await requireCompanyRole("Owner","Admin","Manager","Office","Crew");const raw=(await searchParams).date;const date=raw&&!Number.isNaN(new Date(`${raw}T12:00:00`).getTime())?new Date(`${raw}T12:00:00`):new Date();const data=await getDispatchData(context.companyId,date,context.role==="Crew"?context.user.id:undefined);return <AppLayout><DispatchCenter initial={data}/></AppLayout>}
