import AppLayout from"@/components/layout/AppLayout";import PlanCards from"@/features/billing/PlanCards";import{requireTenantContext}from"@/lib/auth/tenant";import{getCompanyEntitlements}from"@/lib/billing/entitlements";export default async function Page(){const c=await requireTenantContext();const e=await getCompanyEntitlements(c.companyId);return <AppLayout><main className="mx-auto max-w-6xl p-6 sm:p-10"><h1 className="text-4xl font-bold">Plans built for junk removal teams</h1><p className="mt-2 text-slate-600">Pricing and payment details are shown securely in Stripe Checkout.</p><div className="mt-8"><PlanCards current={e.subscription?.plan}/></div></main></AppLayout>}
