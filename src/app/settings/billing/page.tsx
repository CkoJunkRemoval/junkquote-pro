import AppLayout from"@/components/layout/AppLayout";import Link from"next/link";import{requireTenantContext}from"@/lib/auth/tenant";import{getCompanyEntitlements}from"@/lib/billing/entitlements";import{openBillingPortalAction}from"@/app/actions/billing/billing";
export default async function Page(){const c=await requireTenantContext();if(c.role!=="Owner"&&!c.membership.billingAdmin)throw new Error("Billing access is restricted to owners and billing administrators.");const e=await getCompanyEntitlements(c.companyId);const s=e.subscription;return <AppLayout><main className="mx-auto max-w-4xl p-6 sm:p-10"><h1 className="text-3xl font-bold">Billing & subscription</h1>{!e.allowed&&<div className="mt-5 rounded-xl border border-red-300 bg-red-50 p-4"><strong>Restricted mode</strong><p>Existing data remains available to view and export. New estimates, jobs, invoices, approvals, and users are disabled.</p></div>}{s?.status==="PastDue"&&<div className="mt-5 rounded-xl border border-amber-300 bg-amber-50 p-4">Payment failed. Update your payment method before the grace period ends.</div>}<section className="mt-6 rounded-2xl border bg-white p-6"><dl className="grid gap-4 sm:grid-cols-2"><Field label="Plan" value={s?.plan??"No plan"}/><Field label="Status" value={s?.status??"Inactive"}/><Field label="Trial ends" value={date(s?.trialEnd)}/><Field label="Trial remaining" value={`${e.trialDaysRemaining} days`}/><Field label="Renews" value={date(s?.currentPeriodEnd)}/><Field label="Cancellation" value={s?.cancelAtPeriodEnd?"Cancels at period end":"Not scheduled"}/></dl><h2 className="mt-6 text-xl font-bold">Included features</h2><p className="mt-2 capitalize text-slate-600">{e.config.features.join(" · ")}</p><div className="mt-6 flex gap-3"><Link href="/pricing" className="rounded bg-blue-700 px-4 py-2 font-semibold text-white">Change plan</Link>{s?.stripeCustomerId&&<form action={openBillingPortalAction}><button className="rounded border px-4 py-2 font-semibold">Manage billing</button></form>}</div></section></main></AppLayout>}function Field({label,value}:{label:string;value:string}){return <div><dt className="text-sm text-slate-500">{label}</dt><dd className="font-semibold">{value}</dd></div>}function date(x:Date|null|undefined){return x?x.toLocaleDateString():"—"}
