import Link from"next/link";import{redirect}from"next/navigation";import PortalPage from"@/features/portal/PortalPage";import{getCustomerPortalContext}from"@/lib/portal/context";import{listPortalInvoices}from"@/lib/portal/data";const m=(n:number)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(n);
export default async function Page(){const c=await getCustomerPortalContext();if(!c)redirect("/portal/sign-in");const rows=await listPortalInvoices(c.companyId,c.customerId);return <PortalPage company={c.company}><h1 className="text-3xl font-bold">Invoices</h1><div className="mt-5 space-y-3">{rows.map(x=><Link className="block rounded-xl border bg-white p-5" href={`/portal/invoices/${x.id}`} key={x.id}><b>{x.displayNumber??"Invoice"}</b><p>{x.status} · Total {m(x.total)} · Balance {m(x.balanceDue)}</p></Link>)}{!rows.length&&<p>No invoices yet.</p>}</div></PortalPage>}
