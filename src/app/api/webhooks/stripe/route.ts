import{NextResponse}from"next/server";import{getStripe,requireStripeWebhookSecret}from"@/lib/billing/stripe";import{processStripeEvent}from"@/lib/billing/stripeLifecycle";import{recordAuditEvent}from"@/lib/audit/audit";
export async function POST(request:Request){try{const signature=request.headers.get("stripe-signature");if(!signature)return NextResponse.json({error:"Missing Stripe signature."},{status:400});const event=getStripe().webhooks.constructEvent(await request.text(),signature,requireStripeWebhookSecret());const result=await processStripeEvent(event);if(result.companyId)await recordAuditEvent({companyId:result.companyId,eventType:event.type==="invoice.payment_failed"?"billing.payment_failed":"billing.stripe_event",entityType:"Subscription",entityId:event.id,metadata:{type:event.type,duplicate:result.duplicate}});return NextResponse.json({received:true,duplicate:result.duplicate})}catch(error){console.error("[stripe-webhook] processing failed",{message:error instanceof Error?error.message:"Unknown error"});return NextResponse.json({error:"Invalid or unprocessable Stripe webhook."},{status:400})}}
