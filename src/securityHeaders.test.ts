import{describe,expect,it,vi}from"vitest";describe("security headers",()=>{it("configures CSP and browser protections",async()=>{vi.stubEnv("NODE_ENV","production");const{default:config}=await import("../next.config");const groups=await config.headers!();const headers=groups[0].headers;const names=headers.map(x=>x.key);expect(names).toEqual(expect.arrayContaining(["Content-Security-Policy","X-Frame-Options","Referrer-Policy","Permissions-Policy","Strict-Transport-Security","X-Content-Type-Options"]));vi.unstubAllEnvs()})});
