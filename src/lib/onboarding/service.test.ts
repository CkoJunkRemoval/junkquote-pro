import{describe,expect,it,vi}from"vitest";vi.mock("@/lib/prisma",()=>({prisma:{}}));vi.mock("@/lib/distributed/locks",()=>({withDistributedLock:vi.fn()}));import{onboardingProgress,validateOnboardingProfile}from"./service";
describe("first-run onboarding",()=>{it("calculates checklist progress without duplicate inflation",()=>expect(onboardingProgress(["profile","profile","pricing"])).toBe(22));it("validates required company details",()=>expect(()=>validateOnboardingProfile({name:"",serviceRadiusMiles:25})).toThrow("Company name"));it("rejects unsafe websites and invalid service radii",()=>{expect(()=>validateOnboardingProfile({name:"Acme",website:"javascript:alert(1)",serviceRadiusMiles:25})).toThrow("http");expect(()=>validateOnboardingProfile({name:"Acme",serviceRadiusMiles:1001})).toThrow("1,000")});it("accepts a valid first-run profile",()=>expect(()=>validateOnboardingProfile({name:"Acme Hauling",email:"owner@example.com",website:"https://example.com",serviceRadiusMiles:40})).not.toThrow())});
