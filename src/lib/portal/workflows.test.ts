import{describe,expect,it,vi}from"vitest";vi.mock("@/lib/prisma",()=>({prisma:{}}));import{validateMessageAttachments}from"./workflows";
describe("portal workflow security",()=>{it("accepts bounded customer-safe attachments",()=>expect(()=>validateMessageAttachments([{fileName:"photo.jpg",mimeType:"image/jpeg",size:100,url:"/signed/photo"}])).not.toThrow());it("rejects unsafe, oversized, and excessive attachments",()=>{expect(()=>validateMessageAttachments([{fileName:"x.exe",mimeType:"application/x-msdownload",size:1,url:"/x"}])).toThrow("not allowed");expect(()=>validateMessageAttachments([{fileName:"x.pdf",mimeType:"application/pdf",size:11*1024*1024,url:"/x"}])).toThrow("not allowed");expect(()=>validateMessageAttachments(Array.from({length:6},(_,i)=>({fileName:`${i}.pdf`,mimeType:"application/pdf",size:1,url:`/${i}`})))).toThrow("5")})});
