import {describe,expect,it} from "vitest";import{calculateCrewWorkload,calculateDispatchMetrics,filterDispatchJobs,type DispatchJob}from"./calculations";
const job=(overrides:Partial<DispatchJob>={}):DispatchJob=>({id:"j",status:"Scheduled",priority:"Normal",scheduledStart:new Date("2026-07-20T13:00Z"),scheduledEnd:new Date("2026-07-20T15:00Z"),assignments:[{crew:{id:"c",name:"Crew"},employee:null}],invoice:null,...overrides});
describe("dispatch calculations",()=>{it("calculates workload, gaps, overlaps, and capacity",()=>{const result=calculateCrewWorkload([job(),job({id:"j2",scheduledStart:new Date("2026-07-20T14:00Z"),scheduledEnd:new Date("2026-07-20T18:00Z")})]);expect(result[0]).toMatchObject({assignedHours:6,remainingCapacity:2,overlaps:1});});it("aggregates operational metrics",()=>{const result=calculateDispatchMetrics([job({status:"Completed",completedAt:new Date("2026-07-20T14:30Z"),invoice:{total:200,balanceDue:50,status:"Partial"}})],150,new Date("2026-07-20T16:00Z"));expect(result).toMatchObject({jobsToday:1,revenueToday:150,completedJobs:1,averageJobDuration:90,unpaidBalance:50});});it("combines persisted filters and recurring display",()=>{const jobs=[{...job(),customer:{firstName:"Ada",lastName:"Lovelace"},servicePlanId:"p"}];expect(filterDispatchJobs(jobs,{crewId:"c",recurring:"yes",customer:"ada",priority:"Normal"})).toHaveLength(1);expect(filterDispatchJobs(jobs,{recurring:"no"})).toHaveLength(0);});});
