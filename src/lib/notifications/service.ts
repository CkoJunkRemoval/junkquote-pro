import{prisma}from"@/lib/prisma";import{sendOrEnqueueCommunication}from"@/lib/communications/queueCommunication";export async function createNotification(input:{companyId:string;userId?:string;channel:"in-app"|"email"|"sms"|"push";title:string;body:string;to?:string}){const row=await prisma.systemNotification.create({data:{companyId:input.companyId,userId:input.userId,channel:input.channel,title:input.title,body:input.body}});if(input.channel==="email"&&input.to)await sendOrEnqueueCommunication(input.companyId,{channel:"email",to:input.to,subject:input.title,body:input.body,idempotencyKey:`notification:${row.id}`});return row}
