import type{MembershipRole}from"@/generated/prisma/client";export function canManageBilling(role:MembershipRole,billingAdmin:boolean){return role==="Owner"||role==="Admin"||billingAdmin}export function assertCanManageBilling(role:MembershipRole,billingAdmin:boolean){if(!canManageBilling(role,billingAdmin))throw new Error("Only company administrators or billing administrators can manage billing.")}
