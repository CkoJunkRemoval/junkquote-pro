import{describe,it,expect,vi}from"vitest";vi.mock("@/lib/prisma",()=>({prisma:{}}));import{deriveAccess}from"./entitlements";const base={plan:"Starter"as const,trialEnd:null,gracePeriodEnd:null};describe("subscription access",()=>{const now=new Date("2026-01-10");it("allows trials",()=>expect(deriveAccess({...base,status:"Trialing",trialEnd:new Date("2026-01-11")},now).allowed).toBe(true));it("allows active",()=>expect(deriveAccess({...base,status:"Active"},now).allowed).toBe(true));it("restricts expired trials",()=>expect(deriveAccess({...base,status:"Trialing",trialEnd:new Date("2026-01-09")},now).restricted).toBe(true));it("allows past due grace",()=>expect(deriveAccess({...base,status:"PastDue",gracePeriodEnd:new Date("2026-01-12")},now).allowed).toBe(true));it("restricts canceled",()=>expect(deriveAccess({...base,status:"Canceled"},now).restricted).toBe(true));});
