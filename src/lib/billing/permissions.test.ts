import{describe,it,expect}from"vitest";import{assertCanManageBilling,canManageBilling}from"./permissions";describe("billing permissions",()=>{it("allows owners and explicit billing admins",()=>{expect(canManageBilling("Owner",false)).toBe(true);expect(canManageBilling("Office",true)).toBe(true)});it("rejects regular members",()=>expect(()=>assertCanManageBilling("Manager",false)).toThrow("owners or billing administrators"));});
