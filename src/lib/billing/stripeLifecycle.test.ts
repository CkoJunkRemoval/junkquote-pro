import{describe,it,expect,vi}from"vitest";const mocks=vi.hoisted(()=>({findUnique:vi.fn(),create:vi.fn()}));vi.mock("@/lib/prisma",()=>({prisma:{stripeWebhookEvent:{findUnique:mocks.findUnique,create:mocks.create}}}));import{mapStripeStatus,processStripeEvent}from"./stripeLifecycle";describe("Stripe lifecycle mapping",()=>{it("maps access-impacting Stripe states",()=>{expect(mapStripeStatus("active")).toBe("Active");expect(mapStripeStatus("past_due")).toBe("PastDue");expect(mapStripeStatus("canceled")).toBe("Canceled")});it("ignores duplicate webhook deliveries",async()=>{mocks.findUnique.mockResolvedValue({id:"stored"});await expect(processStripeEvent({id:"evt_duplicate",type:"invoice.paid"}as never)).resolves.toEqual({duplicate:true});expect(mocks.create).not.toHaveBeenCalled()});});
