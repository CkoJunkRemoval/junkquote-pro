"use client";import{useState}from"react";import{requestPortalMagicLinkAction}from"@/app/actions/portal/portal";export default function PortalSignIn(){const[email,setEmail]=useState("");const[message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const result=await requestPortalMagicLinkAction(email);setMessage(result.message);}return <form onSubmit={e=>void submit(e)} className="mx-auto max-w-md rounded-2xl border bg-white p-6 shadow"><h1 className="text-2xl font-bold">Customer portal</h1><p className="mt-1 text-slate-600">Enter your authorized email for a secure sign-in link.</p><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} className="mt-5 w-full rounded border p-3" placeholder="you@example.com"/><button className="mt-3 w-full rounded bg-blue-700 p-3 font-semibold text-white">Email sign-in link</button>{message&&<p role="status" className="mt-4 text-sm text-green-700">{message}</p>}</form>}
