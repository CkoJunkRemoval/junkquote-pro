"use client";
import Link from "next/link";
import { useState } from "react";
import { updateInvoiceStatusAction } from "@/app/actions/invoices/updateInvoiceStatus";
import { downloadInvoicePdfAction } from "@/app/actions/invoices/downloadInvoicePdf";
import { downloadPdf } from "@/data/output/downloadPdf";
import { invoiceStatusTransitions, type InvoiceWorkflowStatus } from "@/lib/invoices/statusWorkflow";
import { sendInvoiceAction, updateDraftInvoiceAction } from "@/app/actions/invoices/invoiceMutations";
import type { getInvoiceDetail } from "@/lib/invoices/getInvoiceDetail";
type InvoiceData = NonNullable<Awaited<ReturnType<typeof getInvoiceDetail>>>; const money = (value: number) => new Intl.NumberFormat("en-US", { style: "currency", currency: "USD" }).format(value);

export default function InvoiceDetail({ initialInvoice }: { initialInvoice: InvoiceData }) { const [invoice, setInvoice] = useState(initialInvoice); const [error, setError] = useState<string | null>(null); const [message,setMessage]=useState<string|null>(null); const [saving, setSaving] = useState(false); const [items,setItems]=useState(invoice.lineItems.map(x=>({description:x.description,kind:x.kind,quantity:x.quantity,unitPrice:x.unitPrice}))); const [tax,setTax]=useState(String(invoice.tax)); const [discounts,setDiscounts]=useState(String(invoice.discounts)); const [notes,setNotes]=useState(invoice.notes); async function setStatus(status: InvoiceWorkflowStatus) { setSaving(true); setError(null); try { const updated = await updateInvoiceStatusAction(invoice.id, status); setInvoice((current) => ({ ...current, ...updated })); } catch (e) { setError(e instanceof Error ? e.message : "Unable to update invoice."); } finally { setSaving(false); } } async function send(){setSaving(true);setError(null);try{const updated=await sendInvoiceAction(invoice.id);setInvoice(current=>({...current,...updated}));setMessage(`Invoice emailed to ${updated.lastSentTo}.`)}catch(e){setError(e instanceof Error?e.message:"Unable to send invoice.")}finally{setSaving(false)}} async function save(){setSaving(true);setError(null);try{const updated=await updateDraftInvoiceAction(invoice.id,{lineItems:items,tax:Number(tax),discounts:Number(discounts),dueDate:invoice.dueDate,notes});setInvoice(current=>({...current,...updated}));setMessage("Draft invoice saved.")}catch(e){setError(e instanceof Error?e.message:"Unable to save invoice.")}finally{setSaving(false)}} async function download() { try { downloadPdf(await downloadInvoicePdfAction(invoice.id), `invoice-${invoice.invoiceNumber}.pdf`); } catch (e) { setError(e instanceof Error ? e.message : "Unable to download invoice PDF."); } } const transitions = invoiceStatusTransitions[invoice.status as InvoiceWorkflowStatus]; return <div className="mx-auto max-w-6xl p-6 sm:p-10"><Link href="/invoices" className="text-sm font-semibold text-blue-700">Back to Invoices</Link><div className="mt-3 flex flex-wrap justify-between gap-4"><div><h1 className="text-3xl font-bold">{invoice.displayNumber ?? `Invoice #${invoice.invoiceNumber}`}</h1><p className="text-slate-600">{invoice.customer.firstName} {invoice.customer.lastName} · {invoice.property.address}</p></div><div className="flex gap-2"><button onClick={()=>void send()} disabled={saving||invoice.status==="Void"||invoice.status==="Cancelled"} className="rounded-lg bg-blue-700 px-4 py-2 font-semibold text-white">Email Invoice</button><button onClick={()=>void download()} className="rounded-lg border px-4 py-2 font-semibold">Download PDF</button></div></div>{error&&<p className="mt-4 text-red-600">{error}</p>}{message&&<p className="mt-4 text-green-700">{message}</p>}<section className="mt-6 rounded-2xl border bg-white p-6"><h2 className="text-xl font-bold">Status</h2><div className="mt-3 flex flex-wrap gap-3"><span className="rounded-full bg-slate-100 px-3 py-2">{invoice.status==="Partial"?"Partially Paid":invoice.status}</span>{transitions.filter(x=>x!=="Sent").map(status=><button key={status} disabled={saving} onClick={()=>void setStatus(status)} className="rounded-lg border px-4 py-2">{status==="Void"?"Void Invoice":`Mark ${status}`}</button>)}</div></section><section className="mt-6 grid gap-5 lg:grid-cols-2"><div className="rounded-2xl border bg-white p-6"><h2 className="text-xl font-bold">Customer & service</h2><p className="mt-3 font-semibold">{invoice.customer.firstName} {invoice.customer.lastName}</p><p>{invoice.customer.phone}</p><p>{invoice.customer.email}</p><p className="mt-3">{invoice.property.address}, {invoice.property.city}, {invoice.property.state} {invoice.property.zip}</p>{invoice.job?.scheduledStart&&<p className="mt-3">Job date: {new Date(invoice.job.scheduledStart).toLocaleDateString()}</p>}</div><div className="rounded-2xl border bg-white p-6"><h2 className="text-xl font-bold">Totals</h2><p className="mt-3">Subtotal <span className="float-right">{money(invoice.subtotal)}</span></p><p>Tax <span className="float-right">{money(invoice.tax)}</span></p><p>Discounts <span className="float-right">{money(invoice.discounts)}</span></p><p className="mt-2 border-t pt-2 text-lg font-bold">Total <span className="float-right">{money(invoice.total)}</span></p><p className="font-semibold">Balance due <span className="float-right">{money(invoice.balanceDue)}</span></p><p className="mt-2 text-sm">Due {invoice.dueDate?new Date(invoice.dueDate).toLocaleDateString():"on receipt"}</p></div></section><section className="mt-6 rounded-2xl border bg-white p-6"><h2 className="text-xl font-bold">Line items</h2><div className="mt-3 space-y-2">{items.map((item,index)=><div key={index} className="grid gap-2 md:grid-cols-[1fr_100px_140px_auto]">{invoice.status==="Draft"?<><input aria-label={`Description ${index+1}`} className="rounded border p-2" value={item.description} onChange={e=>setItems(rows=>rows.map((x,i)=>i===index?{...x,description:e.target.value}:x))}/><input aria-label={`Quantity ${index+1}`} type="number" className="rounded border p-2" value={item.quantity} onChange={e=>setItems(rows=>rows.map((x,i)=>i===index?{...x,quantity:Number(e.target.value)}:x))}/><input aria-label={`Price ${index+1}`} type="number" step="0.01" className="rounded border p-2" value={item.unitPrice} onChange={e=>setItems(rows=>rows.map((x,i)=>i===index?{...x,unitPrice:Number(e.target.value)}:x))}/><button onClick={()=>setItems(rows=>rows.filter((_,i)=>i!==index))} className="text-red-700">Remove</button></>:<><span>{item.description}</span><span>{item.quantity}</span><span>{money(item.unitPrice)}</span><strong>{money(item.quantity*item.unitPrice)}</strong></>}</div>)}</div>{invoice.status==="Draft"&&<><button onClick={()=>setItems(rows=>[...rows,{description:"",kind:"Service",quantity:1,unitPrice:0}])} className="mt-3 rounded border px-3 py-2">Add line</button><div className="mt-4 grid gap-3 sm:grid-cols-2"><label>Tax<input type="number" step=".01" value={tax} onChange={e=>setTax(e.target.value)} className="ml-2 rounded border p-2"/></label><label>Discounts<input type="number" step=".01" value={discounts} onChange={e=>setDiscounts(e.target.value)} className="ml-2 rounded border p-2"/></label></div><textarea aria-label="Invoice notes" value={notes} onChange={e=>setNotes(e.target.value)} className="mt-3 min-h-24 w-full rounded border p-3"/><button disabled={saving} onClick={()=>void save()} className="mt-3 rounded bg-slate-900 px-4 py-2 font-semibold text-white">Save Draft</button></>}</section><section className="mt-6 rounded-2xl border bg-white p-6"><h2 className="text-xl font-bold">Timeline</h2><ul className="mt-3 text-sm text-slate-600"><li>Created: {new Date(invoice.createdAt).toLocaleString()}</li>{invoice.sentAt&&<li>Sent: {new Date(invoice.sentAt).toLocaleString()}</li>}{invoice.viewedAt&&<li>Viewed: {new Date(invoice.viewedAt).toLocaleString()}</li>}{invoice.paidDate&&<li>Paid: {new Date(invoice.paidDate).toLocaleString()}</li>}{invoice.voidedAt&&<li>Voided: {new Date(invoice.voidedAt).toLocaleString()}</li>}</ul></section></div>; }
